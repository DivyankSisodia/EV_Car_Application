import 'package:flutter/material.dart';

class SafetySecurityPage extends StatelessWidget {
  const SafetySecurityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
